import { Text } from "react-native";
import { Screen } from "../../../shared/components/Screen";

export function CheckpointScreen() {
  return (
    <Screen>
      <Text>Checkpoint status will show reached and pending riders.</Text>
    </Screen>
  );
}
